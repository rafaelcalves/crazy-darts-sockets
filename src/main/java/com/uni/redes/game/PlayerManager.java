package com.uni.redes.game;

public class PlayerManager {
    private int id;
    public String handleAction(String serverMessage){
        if(serverMessage.equals("WAIT")){

        } else if (serverMessage.equals("GAMEOVER")) {

        }
        return "";
    }
}
